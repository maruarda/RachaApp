<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/app_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/app_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.RachaApp"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- Entry Point -->
        <activity android:name="com.rachapp.ui.activities.LoginActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Other Activities -->
        <activity android:name="com.rachapp.ui.activities.CadastroActivity" />
        <activity android:name="com.rachapp.ui.activities.HomeActivity" />
        <activity android:name="com.rachapp.ui.activities.ResumoFinanceiroActivity" />
        <activity android:name="com.rachapp.ui.activities.CriarRachaActivity" />
        <activity android:name="com.rachapp.ui.activities.RachaDetailActivity" />
        <activity android:name="com.rachapp.ui.activities.MeusRachasActivity" />
        
    </application>

</manifest>